const BASE_REF_NAME = 'vue-pincode-input';
const REGEX_RULE = {
  NUMBER_REGEXP: /^\d{1}$/,
  LETTER_REGEXP: /^\w{1}$/,
  SPECIAL_CHARS_REGEXP: /^\W{1}$/,
};
// const
export { BASE_REF_NAME, REGEX_RULE };
